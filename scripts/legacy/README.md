# scripts/legacy

历史脚本：仅供参考，**默认不上 CI / 不在主流程调用**。

| 文件          | 说明                                                                 |
| ------------- | -------------------------------------------------------------------- |
| `crawl_xhs.py`| 小红书 HTTP 爬虫，来自原 `tester/auto-poster` 仓库。**风控较严**。 |

主流程素材来源已切换到 OpenNana（见 `scripts/opennana_fetch.py`）。
